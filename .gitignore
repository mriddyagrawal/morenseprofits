# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Data caches & results (huge, derivable from jugaad-data)
# Wildcard so a CACHE_VERSION bump (which spawns data/cache.v0/) is also ignored.
data/cache*/
data/results/
*.parquet
!tests/fixtures/*.parquet

# OS / editor / tooling state
.DS_Store
.idea/
.vscode/
*.swp
.claude/

# Streamlit
.streamlit/secrets.toml

# Operator-side manual downloads holding the original NSE archive
# ZIPs (Reports-Archives-Multiple-DDMMYYYY.zip). The unzipped
# .csv.gz sidecar files DO get committed to data/manual/contracts/
# in MIGRATION Phase 0 — those are repo-fixtures, this folder is
# the raw operator-downloaded bundle and shouldn't be tracked.
ONE OFF REPORTS/
