# Pinned to versions that produced a passing Phase-0 smoke test on 2026-05-24.
# Bump deliberately, not by accident.

# Data
jugaad-data==0.33.1
pandas==3.0.3
numpy==2.4.6
pyarrow>=15.0.0   # parquet engine

# Web
streamlit>=1.32.0
plotly>=5.20.0    # heatmaps + trend charts; hover-tooltip composition
                  # via customdata is the killer feature for the research
                  # UI (per DESIGN_SPEC §2.1). Replaces altair, which was
                  # never actually imported in src/ or tests/.

# Testing
pytest>=8.0.0

# Long-running scripts (e.g. scripts/prefetch_universe.py)
tqdm>=4.66.0
