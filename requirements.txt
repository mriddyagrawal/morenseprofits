# Pinned to versions that produced a passing Phase-0 smoke test on 2026-05-24.
# Bump deliberately, not by accident.

# Data
jugaad-data==0.33.1
pandas==3.0.3
numpy==2.4.6
pyarrow>=15.0.0   # parquet engine

# Web
streamlit>=1.32.0
altair>=5.0.0     # streamlit's default chart lib; explicit for clarity

# Testing
pytest>=8.0.0
